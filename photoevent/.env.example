# Domain Configuration
DOMAIN=yourdomain.com
ACME_EMAIL=admin@yourdomain.com

# Database
DB_NAME=eventphotos
DB_USER=postgres
DB_PASSWORD=your-secure-db-password-change-this

# MinIO (Object Storage)
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=your-secure-minio-password-change-this
MINIO_BUCKET=photos

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-string

# Traefik Dashboard Auth (htpasswd format)
# Generate: echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_AUTH=admin:$$apr1$$xxxxxx

# Grafana
GRAFANA_USER=admin
GRAFANA_PASSWORD=your-grafana-password-change-this

# Application Settings (ใส่ตาม business ของคุณ)
PHOTO_PRICE=50
WATERMARK_TEXT=YourBrand
MAX_UPLOAD_SIZE_MB=50
